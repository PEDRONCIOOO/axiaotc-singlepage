'use client'

export default function IframeComponent() {
  return (
    <iframe
      src="https://reserve.fact.finance/tokeniza"
      title="Prova de Reservas em tempo real"
      className="w-full h-full border-0"
      allowFullScreen
    />
  );
}